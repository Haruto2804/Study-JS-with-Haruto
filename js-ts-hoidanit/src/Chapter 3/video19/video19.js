console.log("Video 19");

// 2 dạng tham chiếu dùng nhiều nhất

// 1. Object tương tự struct bên C++
const person = {
    name: "haruto",
    age: 20,
    address: "TPHCM"
}
console.log(person);

//array, array bên JS dùng dấu []
const scores = [10, 3, 4, 9]
console.log(scores);